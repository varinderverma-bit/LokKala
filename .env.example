# Base URL of the API Gateway (or CloudFront) for the art marketplace backend.
# When set, the UI loads artifact list and details from the LokKala BFF; images use CDN URLs.
# Example after deploy: https://xxxxxxxx.execute-api.region.amazonaws.com
VITE_API_BASE_URL=
